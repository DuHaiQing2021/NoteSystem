# 开发环境的配置文件
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/notesystem?characterEncoding=utf8&&useSSL=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

#保存路径
mybatis:
  configuration: #配置打印mybatis执行的SQL
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

#配置打印mybatis执行的SQL
logging:
  level:
    com:
      example:
        demo: debug